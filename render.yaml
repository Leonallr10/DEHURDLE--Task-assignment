services:
  - type: web
    name: dehurdle-api
    runtime: node
    rootDir: backend
    buildCommand: chmod +x render-build.sh start.sh && bash render-build.sh
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        sync: false
